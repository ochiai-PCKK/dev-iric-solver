RRI Result Viewer

RRIが出力する時系列ASCラスターを読み込み、iRICの可視化ウィンドウでスライダー再生できるCGNS結果として書き出す計算結果分析ソルバー。

使い方（概要）
- 画面の設定に従ってASCフォルダや時系列条件を指定する。
- 出力は output_folder/<YYYYMMDD_HHMMSS>/ に作成される。

注意
- 入力はASCで、そこから格子を生成する。
- 1次元配列は行優先（C順）で出力する。
- ASCの行順に合わせるため、通常は「Y 方向を反転 (行順) = はい」を推奨。
- NODATAはNaNに変換して出力する。
